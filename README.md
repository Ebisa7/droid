# Minimal Android Compose Starter

A minimal Android application built with Jetpack Compose.

## Build & Run

```bash
./gradlew assembleDebug
```

To install on device:
```bash
./gradlew installDebug
```